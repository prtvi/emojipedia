{
	"name": "emojipedia",
	"version": "1.0.0",
	"description": "An emoji dictionary",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server/index.js",
		"build": "cd client && npm install && npm run build"
	},
	"author": "prtvi",
	"license": "ISC",
	"dependencies": {
		"express": "^4.18.2"
	},
	"engines": {
		"node": "16.16.0"
	}
}
